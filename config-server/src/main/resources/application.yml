server:
  port: 8888
spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/DanielCuadradoGallardo/bootcamp-microservicios-dia-1.git
        default-label: main
        search-paths:
        - '*service*'

